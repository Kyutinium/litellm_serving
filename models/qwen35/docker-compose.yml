services:
  chatdragon-instant-vllm:
    image: vllm/vllm-openai:v0.20.1
    container_name: chatdragon-instant-vllm
    ipc: host
    ports:
      - "2598:10046"
    volumes:
      - /PATH/TO/CKPT:/model:ro
    command:
      - --model
      - /model
      - --tensor_parallel_size=2
      - --port
      - "10046"
      - --reasoning-parser
      - qwen3
      - --served-model-name
      - ChatDRAGON-Instant
      - --max-model-len
      - "200000"
      - --max-num-seqs
      - "32"
      - --enable-auto-tool-choice
      - --tool-call-parser
      - qwen3_coder
      - --enable-prefix-caching
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["0", "1"]
              capabilities: [gpu]
    restart: unless-stopped
